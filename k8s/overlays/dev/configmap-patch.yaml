# 개발 환경에서 base ConfigMap 위에 덮어쓸 값
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-auth-config
data:
  APP_ENV: "development"
  # 개발용 Cognito User Pool / Client 로 채워주세요.
  COGNITO_USER_POOL_ID: "ap-northeast-2_DEVxxxxxx"
  COGNITO_CLIENT_ID: "dev-client-id-xxxxxxxx"
